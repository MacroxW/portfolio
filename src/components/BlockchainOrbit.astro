<div class="chain-visual" aria-hidden="true">
  <div class="chain-glow"></div>
  <svg viewBox="0 0 360 360" role="presentation">
    <defs>
      <linearGradient id="chain-line" x1="0" y1="0" x2="1" y2="1">
        <stop offset="0" stop-color="#fbbf24" />
        <stop offset=".52" stop-color="#22d3ee" />
        <stop offset="1" stop-color="#a78bfa" />
      </linearGradient>
      <radialGradient id="chain-core">
        <stop offset="0" stop-color="#fef3c7" />
        <stop offset=".3" stop-color="#f59e0b" />
        <stop offset="1" stop-color="#92400e" />
      </radialGradient>
      <filter id="chain-blur"><feGaussianBlur stdDeviation="3" /></filter>
    </defs>

    <g class="network-lines" fill="none" stroke="url(#chain-line)" stroke-width="1.2">
      <path d="M180 72 282 132 274 248 174 297 77 235 82 124Z" />
      <path d="M180 72 174 297M82 124 274 248M282 132 77 235" opacity=".45" />
      <circle cx="180" cy="180" r="126" stroke-dasharray="4 10" opacity=".35" />
      <circle cx="180" cy="180" r="91" stroke-dasharray="2 8" opacity=".25" />
    </g>

    <g class="orbit orbit-one">
      <circle cx="180" cy="54" r="4" fill="#fbbf24" />
      <circle cx="180" cy="54" r="10" fill="#fbbf24" opacity=".18" filter="url(#chain-blur)" />
    </g>
    <g class="orbit orbit-two">
      <circle cx="180" cy="89" r="3" fill="#22d3ee" />
      <circle cx="180" cy="89" r="9" fill="#22d3ee" opacity=".2" filter="url(#chain-blur)" />
    </g>

    <g class="blocks">
      <g class="block block-a" transform="translate(180 72)"><rect x="-15" y="-15" width="30" height="30" rx="7" /><path d="M-7 0 0-6 7 0 0 6Z" /></g>
      <g class="block block-b" transform="translate(282 132)"><rect x="-13" y="-13" width="26" height="26" rx="6" /><path d="M-6 0 0-5 6 0 0 5Z" /></g>
      <g class="block block-c" transform="translate(274 248)"><rect x="-13" y="-13" width="26" height="26" rx="6" /><path d="M-6 0 0-5 6 0 0 5Z" /></g>
      <g class="block block-d" transform="translate(174 297)"><rect x="-14" y="-14" width="28" height="28" rx="6" /><path d="M-6 0 0-5 6 0 0 5Z" /></g>
      <g class="block block-e" transform="translate(77 235)"><rect x="-13" y="-13" width="26" height="26" rx="6" /><path d="M-6 0 0-5 6 0 0 5Z" /></g>
      <g class="block block-f" transform="translate(82 124)"><rect x="-13" y="-13" width="26" height="26" rx="6" /><path d="M-6 0 0-5 6 0 0 5Z" /></g>
    </g>

    <g class="core">
      <circle cx="180" cy="180" r="52" fill="#111827" stroke="url(#chain-line)" stroke-width="1.5" />
      <circle cx="180" cy="180" r="38" fill="url(#chain-core)" opacity=".16" />
      <path d="M180 142 211 160v38l-31 18-31-18v-38Z" fill="none" stroke="#fbbf24" stroke-width="2" />
      <path d="m149 160 31 18 31-18M180 178v38" fill="none" stroke="#22d3ee" stroke-width="1.5" />
      <circle cx="180" cy="180" r="5" fill="#fff" />
    </g>
  </svg>
  <span class="chain-label label-one">BLOCK</span>
  <span class="chain-label label-two">VERIFY</span>
  <span class="chain-label label-three">SYNC</span>
</div>

<style>
  .chain-visual {
    position: relative;
    width: min(100%, 22rem);
    aspect-ratio: 1;
    isolation: isolate;
    border: 1px solid rgb(255 255 255 / 0.1);
    border-radius: 2.5rem;
    overflow: hidden;
    background:
      linear-gradient(rgb(255 255 255 / 0.035) 1px, transparent 1px),
      linear-gradient(90deg, rgb(255 255 255 / 0.035) 1px, transparent 1px),
      radial-gradient(circle at 50% 45%, #1f2937, #080b16 72%);
    background-size: 30px 30px, 30px 30px, auto;
    box-shadow: 0 30px 80px rgb(0 0 0 / 0.35), inset 0 1px rgb(255 255 255 / 0.08);
    transform: perspective(900px) rotateY(-7deg) rotateX(3deg);
  }

  .chain-visual::after {
    content: "";
    position: absolute;
    inset: 0;
    background: linear-gradient(115deg, transparent 25%, rgb(255 255 255 / 0.07) 48%, transparent 70%);
    translate: -110% 0;
    animation: chain-scan 5s ease-in-out infinite;
    pointer-events: none;
  }

  .chain-glow {
    position: absolute;
    z-index: -1;
    inset: 28%;
    border-radius: 999px;
    background: #f59e0b;
    filter: blur(60px);
    opacity: .2;
    animation: chain-breathe 3s ease-in-out infinite;
  }

  svg { width: 100%; height: 100%; overflow: visible; }
  .network-lines { animation: chain-lines 4s ease-in-out infinite; transform-origin: 180px 180px; }
  .orbit { transform-origin: 180px 180px; }
  .orbit-one { animation: chain-orbit 8s linear infinite; }
  .orbit-two { animation: chain-orbit 5.5s linear infinite reverse; }

  .block rect { fill: #111827; stroke: #64748b; stroke-width: 1; }
  .block path { fill: none; stroke: #fbbf24; stroke-width: 1.25; }
  .block { transform-box: fill-box; transform-origin: center; animation: chain-float 3s ease-in-out infinite; }
  .block-b, .block-e { animation-delay: -.6s; }
  .block-c, .block-f { animation-delay: -1.2s; }
  .block-d { animation-delay: -1.8s; }
  .core { transform-origin: 180px 180px; animation: chain-core 6s ease-in-out infinite; }

  .chain-label {
    position: absolute;
    padding: .35rem .55rem;
    border: 1px solid rgb(255 255 255 / .1);
    border-radius: 999px;
    color: rgb(255 255 255 / .55);
    background: rgb(15 23 42 / .78);
    font: 600 .56rem/1 ui-monospace, monospace;
    letter-spacing: .18em;
    backdrop-filter: blur(10px);
    animation: chain-label 4s ease-in-out infinite;
  }
  .label-one { left: 7%; top: 16%; }
  .label-two { right: 6%; top: 38%; animation-delay: -1.3s; }
  .label-three { left: 13%; bottom: 12%; animation-delay: -2.6s; }

  @keyframes chain-orbit { to { rotate: 360deg; } }
  @keyframes chain-float { 50% { translate: 0 -5px; filter: drop-shadow(0 0 8px rgb(251 191 36 / .45)); } }
  @keyframes chain-core { 50% { scale: 1.06; filter: drop-shadow(0 0 14px rgb(34 211 238 / .35)); } }
  @keyframes chain-lines { 50% { opacity: .6; rotate: 2deg; } }
  @keyframes chain-breathe { 50% { scale: 1.35; opacity: .3; } }
  @keyframes chain-label { 50% { translate: 0 -6px; color: rgb(255 255 255 / .85); } }
  @keyframes chain-scan { 45%, 100% { translate: 110% 0; } }

  @media (prefers-reduced-motion: reduce) {
    .chain-visual *, .chain-visual::after { animation: none !important; }
  }
</style>
